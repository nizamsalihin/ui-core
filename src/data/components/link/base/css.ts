export default `.link {
    font: 600 14px/1.2 var(--font-family);
    padding: 0;
    border: none;
    background: none;
    color: var(--primary-color);
    text-decoration: none;
    cursor: pointer;
}`;
